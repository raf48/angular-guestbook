block content
  div(ng-repeat="message in messages | orderBy : orderMessages")
    message(from="{{ message.from }}" text="{{ message.text }}" date="{{ message.date | date : 'yyyy-MM-dd HH:mm:ss' }}")
